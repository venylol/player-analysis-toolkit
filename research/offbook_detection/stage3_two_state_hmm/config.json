{
  "schema": "stage3-two-state-hmm-config-v1",
  "status": "frozen",
  "seed": 42,
  "initialization_seeds": [42, 43, 44, 45, 46],
  "projection_dim": 16,
  "target_decision_min_inclusive": 3,
  "target_decision_max_inclusive": 19,
  "strict_ply_max_inclusive": 38,
  "states": 2,
  "student_t_degrees_of_freedom": 5.0,
  "scale_floor": 0.0001,
  "initial_state_probabilities": [0.5, 0.5],
  "initial_transition_matrix": [[0.9, 0.1], [0.1, 0.9]],
  "initialization": "two-cluster MiniBatchKMeans over all train observations",
  "kmeans_batch_size": 8192,
  "kmeans_max_iter": 100,
  "optimizer": "Adam",
  "learning_rate": 0.003,
  "weight_decay": 0.0,
  "batch_size": 2048,
  "max_epochs": 200,
  "early_stopping_patience": 15,
  "early_stopping_min_delta": 0.00001,
  "selection_metric": "validation marginal NLL per analyzed decision",
  "training_precision": "float32",
  "automatic_mixed_precision": false,
  "state_naming": "train posterior early/late thirds; sequences shorter than 3 excluded from naming only",
  "test_usage": "one joint evaluation of both frozen families after explicit user approval"
}
