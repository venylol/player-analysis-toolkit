# 当前选手对局调查方法

## 一、分析范围

调查开始时必须明确：

- 被调查账号；
- Othello Quest 当前可见对局范围；
- 预先指定的被报告对局；
- 其余对局作为个人对照组；
- 已有 EG 分析的参数及结果目录。

被报告局不能因为分析结果而事后更换。程序不会把统计量转换成“作弊概率”。

## 二、原始事件与实际落子

OQ `position.moves` 中包含三类事件：

- 坐标着手；
- 显式 pass；
- 没有坐标的终局或认输事件。

用时保留原始 `t` 毫秒值。只有坐标着手拥有 EG 子损。pass 和终局事件保留
用时，但不计算子损。

## 三、子损统计

子损直接读取已有 EG `lossClipped`，不在工具箱中重新调用引擎。统一口径为
`disc_loss = max(0, raw_loss)`，并同时报告包含边界的
`loss_ge4 = I(disc_loss >= 4)` 与 `loss_ge10 = I(disc_loss >= 10)`。≥4 是主要
敏感指标，≥10 是更稀少的“大失误”指标；后者区间通常更宽，但不会因此单独赋予
更强证据权重。

### 3.1 两种平均口径

- 局等权：先计算每局平均子损，再让每局权重相同。
- 着手等权：把所有实际着手合并，每一手权重相同。

两者同时输出，用于识别结果是否被个别长对局的着手数量影响。

### 3.2 整局聚类 bootstrap

重采样单位是整局，不是单手。每次分别从被报告局和对照局中有放回抽取相同
数量的整局，再计算差值。95%区间取重采样差值的2.5%和97.5%分位。

### 3.3 精确组合位置

如果被报告组有两局，就枚举同一比较范围内的全部两局组合，计算被报告组合
在这些组合中的升序位置。这是样本内的精确经验位置，不是作弊概率。

### 3.4 单步两部分模型

模型拆分两个问题：

1. 某手是否为零子损；
2. 已经发生正子损时，损失有多大。

零子损部分使用 logistic 模型。正子损部分对正子损取自然对数后做线性回归，
再用 smearing correction 还原到原尺度。两部分均控制线性 ply、执棋颜色
和是否为比赛局。区间通过整局聚类 bootstrap 获得。

### 3.5 从实际落子 ply 39 起的 WLD 损失总和（可选）

该指标只在 CLI 使用 `--wld-from-ply 39`，或配置使用 `"wldFromPly": 39` 时
启用；省略时保持旧输出不变。边界坐标是排除显式 pass 和其他非坐标事件后的
全局实际落子序号，条件为 `global_placement_ply >= 39`，因此第 39 手包含在内。
`move_index`、包含 pass 的源 `ply` 和 `finalStatus` 都不作为该指标的边界或标签。

每个实际落子复用调查链路已有的落子前最佳分数与落子后的局面最佳分数。落子后
分数先转回刚落子一方视角：

```text
正常换边：actual_move_score = -next_best_score
落子后对手 pass、同一方继续：actual_move_score = next_best_score
```

分数大于 0、等于 0、小于 0 分别映射为 Win=2、Draw=1、Loss=0，再计算：

```text
drop = max(0, before_rank - after_rank)
wld_loss = drop / 2
```

所以 Win→Draw 与 Draw→Loss 均为 0.5，Win→Loss 为 1；不变、改善和其他非恶化
变化为 0。引擎实际总和字段是 `engine_wld_loss_total_from_ply39`。模型预测输入
只对 `wld_applicable=true` 且处于边界内的 `expected_wld_loss` 求和，字段是
`predicted_expected_wld_loss_total_from_ply39`。模型输入缺少 `game_id`、
`global_placement_ply`、`wld_applicable`、`expected_wld_loss` 或 mover 身份
`player_id/side` 时明确失败，不补零。

常规 JSON、CSV、Markdown/PDF 和终端摘要只展示每局/棋手及棋手汇总总和；逐着
WLD、变化前后 W/L/D、平均每着 WLD、逆转着数和发生率不对外展示。玩家汇总只
累计该玩家本人落子。该可选汇总不修改 Egaroucid 的 book、level 或其他现有分析
参数。

## 四、用时趋势

用时分析使用原始毫秒，不做对数变换。基线只使用与被报告局同色的个人对照
局，以算术平均为中心拟合带四个固定结点的三次回归样条，ridge alpha 为0.1。

每手残差为：

```text
实际用时 - 同 ply 基线预测用时
```

长考定义为实际用时高于相同 ply 对照分布第90百分位。被报告组合与全部同样
大小的对照局组合比较，并输出 plus-one 经验尾部位置。

## 五、特定选手 Human Frequency Book

### 5.1 建库单位

个人库不保存“只属于选手的坐标字符串”，而保存双方共同形成的完整棋盘节点：

```text
[board_string, occurrence_count, ply]
```

棋盘使用当前行动方视角：`X` 为当前方，`O` 为对手，`-` 为空位；然后在8种
旋转、镜像中取字典序最小表示。

黑棋和白棋分别建库。被报告对局全部排除。

### 5.2 评价某次选择

轮到被调查选手时：

- `parent_book_count`：落子前完整局面的历史次数；
- `book_count`：实际落子后子局面的历史次数；
- `book_hit`：子局面是否有历史先例；
- `parent_child_ratio`：子局面次数除以父局面次数。

结果分为：

- `historical_choice`：父局面和实际子局面都在个人库中；
- `new_player_choice`：父局面存在，但选手走出的子局面未见；
- `unseen_parent`：对手应对或此前分支已使当前局面离开个人库。

对手着手不计入选手命中数，但必须参与完整棋盘重建。

### 5.3 对照局留一法

评价每盘对照局时，该局自身从个人库排除。否则对照局一定能找到自己，命中率
会被系统性抬高。

每局主要输出：

- 历史局面覆盖率；
- 历史选择命中率；
- 从开局开始的连续历史选择数；
- 第一次离库发生在哪次个人选择；
- 第一次离库由个人新选择还是历史未见父局面造成。

## 六、标准定式名称与筛选

标准定式由独立的 `standard_openings.json` 维护，每条记录包含稳定 `id`、显示
名称、着手序列和可选别名。目录只记录人工确认的定式，不根据统计结果自动发明
名称。

匹配使用双方完整实际落子序列。定式序列和对局的同长度前缀分别在8种旋转、
镜像下取规范表示；两者相同即为包含该定式。若一局同时匹配多个嵌套定式，最长
序列作为该局最具体的 `opening`，全部匹配保留在 `openingMatches`。

该功能与个人 Human Frequency Book 分开：标准定式回答公开名称和序列匹配，
个人库回答目标选手是否走过自己的历史选择。

## 七、算法脱谱位置

正式流程在 Level22 完成并通过 audit 后执行确定性脱谱算法。算法只分析目标棋手
本人的实着节点，pass 不参与 ply 或用时历史；候选闭区间固定为 ply 5–38。

当前节点用时严格大于该棋手本局此前全部实着用时中位数的1.75倍时形成用时
候选。Level22 的 `bestEval` 表示落子前当前局面估值；目标棋手节点首次满足
`abs(bestEval) > 6` 时形成估值截断点，正负6恰好相等不触发。用时规则只分析
估值截断点之前的节点；若此前存在用时候选，取最早者，否则取估值截断点；两者
都不存在则标记 `no_offbook`。相对估值、`actualEval` 和着法子损不参与该判断。

每局都在 `offbook_records.json` 中记录 `algorithmLabel`、锚点来源和计算证据，
不再要求 Agent 手工标注。该方法经过人工样本审核，在当前审核结论中比此前的
Agent 审核结果和机器学习生成结果更准确，因此作为正式脱谱登记方法。

脱谱后指标从该标记手开始并包含该手。单独输出：

- 脱谱后局等权和着手等权子损；
- 脱谱后原始毫秒用时的局等权和着手等权 mean；
- 整局指标，用作同一输出中的参照；
- 目标选手与已有脱谱标签的排行榜高分选手参照组之间的差值。

`no_offbook` 对局进入整局统计，但不进入脱谱后单元。所有边界均来自正式算法
记录，统计阶段不会推断或改写脱谱位置。

### 7.1 脱谱后固定四阶段

`offbook-model` 在保留 `fullGame` 和 `postOffBookInclusive` 原定义与字段的同时，
新增 `postOffBookByPhase` 和 `postOffBookPhaseCombined`。固定全局实际落子阶段为：

- ply 1–30，即 `[1,31)`；
- ply 31–47，即 `[31,48)`；
- ply 48–53，即 `[48,54)`；
- ply 54–60，即 `[54,61)`。

程序先按人工 `offBookPly` 硬性排除此前的目标选手节点，且包含脱谱手本身，
然后才把余下节点放入以上阶段。阶段不会按选手、对局、当前数据或 bootstrap
重复动态平移或重新拟合。

EG `ply` 在使用前经过显式语义审计：节点必须全是坐标着手，完整源节点的 ply
必须从 1 连续递增，最大不得超过 60，`sourceMoveIndex` 可以因 pass 或其他非落子
事件跳号。该语义与审查包生成人工 `offBookPly` 所用的 `replay_game()` 相同，
二者都是剔除 pass 后的全局实际落子序号。标记点还必须恰好存在于目标选手 EG
节点中；当前实现不做坐标转换，异常输入直接失败。

每阶段分别报告两组的原始组局数、目标节点数、有效子损节点数、实际贡献局数和
独立局数，并计算局等权平均子损、着手等权平均子损、零子损率、正子损平均值、
子损≥4比例和子损≥10比例；两个阈值还分别给出节点数。六项均输出举报组减
对照组差值和以整局为单位的 95%经验/稳定性
区间。没有 `lossClipped` 的实际落子可计入目标节点覆盖，但不进入子损统计。

节点可以预留 `probability_loss_ge4` 与 `probability_loss_ge10`。只有某阈值的全部
有效子损节点都提供合法概率时，才计算预计节点数、实际节点数减预计节点数，及
按节点/整局归一的实际发生减模型预测概率；缺失时明确记为“模型概率不可用”，
不补 0、不补空字符串，也不在本流程训练模型。

阶段 bootstrap 每次执行以下固定过程：

1. 举报组内有放回抽取与原举报组相同数量的整局；
2. 对照组内有放回抽取与原对照组相同数量的整局；
3. 同一次的两份抽样名单供四阶段、≥4、≥10 和全部其他指标共同使用，一局的全部脱谱后节点及
   所属阶段同时出现；
4. 先计算各阶段两组统计和差值；
5. 在该次重复内部按固定权重合成；
6. 所有重复结束后，才对阶段差值和已合成差值取 2.5%/97.5% 分位。

因此四阶段不是四个独立样本，也不会独立重抽；合成区间不是阶段区间端点的
平均。

同时输出两个固定权重口径：

- 主口径 `referenceHumanExposure` 使用归档独立人类参考样本四阶段有效子损
  节点数 `48631 / 51032 / 18040 / 17921`（总计 `135624`）的比例，即约
  `35.8572% / 37.6276% / 13.3015% / 13.2137%`；来源固定为
  `investigations/oq_loss_phase_boundaries_20260803/ply_statistics_postbook.csv`，
  不读取当前举报/对照组估权；
- 敏感性口径 `equalPhase` 为每阶段 25%，让不同阶段同等重要，但相对提高短
  尾盘阶段的影响。

某阶段在任一组无统计节点时，该阶段相应点估计可以为带原因的 `null`。若某次
有放回抽样暂时缺阶段或缺少某项指标，该项阶段/合成重复记为失败，不重抽、不
删除阶段、不重新分配权重。只有成功重复率达到固定 95% 才发布 95%区间；否则
区间为带原因的 `null`。输出保留每项成功/失败次数、成功率和阶段覆盖诊断。

四阶段边界是用户选定的固定解释细分，不是普适自然边界。归档研究中
`30|47|53` 相对三阶段样本内拟合改善 28.3%，但完整 bootstrap 复现率为 38.4%，
且后两个边界随最小阶段宽度变化。ply 31/32 附近还混有 Egaroucid 搜索制度
变化，不能把阶段差异全部归因于人类行为转折，也不能据此输出作弊概率或可靠
总体边界。

举报组极小时，重复次数不会创造新的独立信息。例如两局举报样本每次抽两局只
有 A+A、A+B、B+B 三类独特组成。阶段区间因此仍只是当前样本下的经验/稳定性
区间；原有精确组合位置、留一局分析和整局统计继续作为解释约束。

## 八、外部参照组

外部排行榜参照按实际采样对局分组。每组输出：

- 局数、着手数；
- 局等权和着手等权平均子损；
- 零子损率、正子损平均值、子损大于等于4的比例；
- Ply 1-20、21-40、41以后分段统计。

如果同一选手贡献多局，结果是“对局组合的经验位置”，不能当作同等数量的独立
选手样本。

## 九、报告呈现

报告应先给出描述性结论，再解释计算口径。开局部分不再使用“只抽取选手坐标后
比较序列”的旧方法。PDF 由 Markdown 通过 GitHub Issue 风格渲染脚本生成，
Markdown 保持为报告的可编辑源文件。

## 十、玩家异常哨兵 V1

正式 Reference 使用双方 oldR 均在1600–2486的1,379局主矩阵，形成2,758条目标方
视角记录；116局低分扩展仍生成双向脱谱和子损记录，但不进入正式统计分母。
每局黑白双方分别调用同一确定性脱谱算法，不共享或人工补充锚点。

每条调查记录按目标方颜色和 `analysisScope` 匹配 Reference。Elo 轴使用各档中心，
最后一档中心为2443；正常情况对目标与对手 oldR 做双线性插值。缺格时先在相同
目标 Elo 档沿对手轴寻找最近格，再寻找最近二维格，不改变颜色或 scope。调查
账号 rating 超界时仅夹到边界并显式输出标记。匹配期望是格内所有单局 GE4 比例
等权、格间按插值权重合成的均值。

单局外部强度残差定义为：

```text
匹配 Reference 预期 loss_ge4_rate - 调查局实际 loss_ge4_rate
```

正值表示调查局子损≥4比例低于同条件 Reference。扫描按该残差降序，平手按 gameId
稳定排序，只检查单局、全部对局以及 k=2 到
`min(floor(n/2), n-3)` 的排序前缀。候选前缀的 `externalEffect` 是前缀残差均值，
`internalEffect` 是前缀残差均值减剩余对局残差均值。

10,000名伪玩家按每个调查槽位的精确 Elo 插值权重、颜色和 scope，从完整 Reference
目标方单局记录中有放回抽样。抽中记录相对该槽位计算外部残差时，全局剔除其
gameId的两个方向。每名伪玩家执行相同完整扫描；每个 k 的联合证据取外部与内部
单侧正常超越率中较大者，再用每名伪玩家自己的最佳完整扫描结果校正选 k 偏差。
有限样本采用 plus-one 修正，95%区间采用 Wilson 方法。

集中型异常还要求选定 k 的两项效应为正，且固定候选后的10,000次整局聚类
bootstrap区间都完全位于正方向。这个 bootstrap 只描述冻结候选条件下的效应；
选择偏差由伪玩家完整扫描负责。WLD 使用同一 GE4 候选、同一双方 Elo 与颜色，
固定包含全局实着 ply 39，只作为不能改变名单的次要指标。

调查局与 Reference 重叠时，gameId 在正式评分前从全部格、统计、bootstrap和伪
玩家池全局剔除。冻结后的 `selection_manifest.json` 记录输入哈希、随机种子、重复
次数、所有 k、最终名单和规范化 payload SHA-256；后续个人模型不得反向修改名单。

当前冻结派生缓存中，正式 `no_offbook` 记录只有黑方1条、白方0条。黑方唯一记录
被伪玩家抽中后，强制留一 gameId 会使同颜色/scope池为空。因此 V1 将调查中的
`no_offbook` 局保留算法和整局 GE4 记录，但明确标为
`not_calibratable`；不跨颜色、不借用 `offbook` 分布，也不静默补值。

## 十一、哨兵模式行棋质量等价 Elo V1

行棋质量等价 Elo 是与异常扫描并存的新估计，不是账号当前显示 Elo、历史最高 Elo、
作弊概率或未来胜率。实现位于 `src/player_analysis_toolkit/sentinel_elo.py`，CLI 位于
`scripts/analysis/sentinel_elo_analysis.py`，配置冻结在
`sentinel_elo_reference_config.json`。

每条 directed game record 同时保存 `full_game` 和
`post_offbook_inclusive` 两套四阶段指标。目标局有脱谱锚点时选择后者并包含锚点，
否则选择前者。阶段边界固定为 global placement ply `[1,30]`、`[31,47]`、
`[48,53]`、`[54,60]`；pass 不消耗新的 global placement ply。阶段内计算
`lossPositive >= 4` 比例，四阶段严格等权；缺任一阶段的局整局排除。

对每个试探整数 Elo `E=1600..2495` 和每个目标局，Reference 只保留正式、同色、
同 scope、四阶段完整且通过账号与 gameId 双向泄漏排除的 directed records。距离为
目标方 `(RT-E)` 与对手 `(RO-O_i)` 的二维欧氏距离，`K=ceil(N^(2/3))`，权重使用
第 `K+1` 条距离作为三角核边界。距离加权均值和总体标准差将目标局整局 GE4 标准化
为 `gameZ`；多个目标局的 `candidateZ` 是 gameZ 的算术平均，不按节点数、输赢或
对手权重调整。

校准采用 leave-one-account-out。已知 Elo 是冻结源 bundle 中该账号按 `created` 最新
详情的赛后 `newR`，只用于校准后核对。账号按固定 SHA-256 种子拆成 calibration 和
独立 validation 集；全局 `T95` 只有在独立 validation 覆盖率达到 95% 且样本门槛满足
时才可启用。结果字段使用 `databaseCalibrated95Range` 和
`databaseCalibrated95Intervals`，不使用会暗示理论正态区间的字段名。

校准在当前快照使用16个账号 worker 并行；每个 worker 内部只使用一个树查询线程，
避免线程过度竞争。完整公式、状态分类、产物哈希和独立覆盖率见
`docs/SENTINEL_ESTIMATED_ELO_ACCEPTANCE_REPORT_20260819.md`。

校准缓存只在显式执行 `calibrate-elo` 时生成或更新；普通 `estimate-elo` 运行直接读取
参考目录中的 `elo_calibration.json`，不会每次重新统计满足至少10局的用户。数据库扩展后，
必须以新的版本化参考目录重新构建 directed phase records、构建审计和
`reference_sha256_manifest.json`，再重新执行 `calibrate-elo`，以更新用户集合、
calibration/validation 划分、`T95`、曲线诊断阈值和校准案例。已有账号若要使用扩展后的
数据库，也要重新运行估计；不得将新参考记录与旧校准缓存混用。该过程目前不自动检测扩展，
也不做增量校准。旧 sentinel 的 scan/freeze 流程不受影响。

单选手调查的联立入口是 `scripts/analysis/sentinel_unified_analysis.py`。它在旧 sentinel
完成 Reference scoring、pseudo scan 和 freeze 后，读取旧流程产物并追加当前账号的预估 Elo，
输出 `sentinel_unified_analysis.json`；`run_player_investigation.py` 只负责阶段编排和最终
报告组装，不在其中实现 Elo 公式。统一调查流程也通过该脚本调用旧 sentinel 的兼容命令；
旧 `sentinel_analysis.py` 可以继续保留给外部兼容调用，但不再是统一调查主入口。未来新增
单选手指标应继续扩展该统一分析脚本。
